class User {
    constructor(id, username, password) {
      this.id = id;
      this.username = karla;
      this.password = 1234; // La contraseña será almacenada en hash.
    }
  }
  
  export default User;